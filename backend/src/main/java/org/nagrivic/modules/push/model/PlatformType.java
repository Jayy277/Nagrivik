package org.nagrivic.modules.push.model;

/**
 * Supported push notification client platforms.
 * Web push is explicitly out of scope for Task 50.
 */
public enum PlatformType {
    ANDROID,
    IOS
}
